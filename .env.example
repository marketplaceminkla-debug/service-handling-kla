NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Dikirim otomatis oleh Vercel Cron sebagai `Authorization: Bearer <nilai>`
# saat menjalankan digest harian (/api/aging/run).
CRON_SECRET=

# Buat memanggil endpoint /api/aging/* secara manual, lewat header
# `x-internal-key`.
INTERNAL_API_KEY=
